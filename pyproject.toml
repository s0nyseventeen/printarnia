[project]
name = 'src'
version = '2.1.7'
description = 'The second release of the Sheikhs application'
dependencies = [
	'flask',
	'psycopg2-binary',
	'gunicorn',
	'Flask-SQLAlchemy',
	'python-dotenv'
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
